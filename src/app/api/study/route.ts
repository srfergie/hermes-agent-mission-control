import {NextRequest,NextResponse} from "next/server";import {prisma} from "@/lib/prisma";export const dynamic="force-dynamic";export async function GET(){return NextResponse.json(await prisma.studyMilestone.findMany({orderBy:[{deadline:"asc"},{updatedAt:"desc"}]}));}export async function POST(r:NextRequest){const b=await r.json();if(!b.track||!b.title||!b.nextAction)return NextResponse.json({error:"track, title and nextAction required"},{status:400});return NextResponse.json(await prisma.studyMilestone.create({data:{track:b.track,title:b.title,nextAction:b.nextAction,deadline:b.deadline?new Date(b.deadline):null,status:b.status||"active"}}),{status:201});}
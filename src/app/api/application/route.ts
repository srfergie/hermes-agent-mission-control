import {NextRequest,NextResponse} from "next/server";import {prisma} from "@/lib/prisma";export const dynamic="force-dynamic";export async function GET(){return NextResponse.json(await prisma.applicationOpportunity.findMany({orderBy:{updatedAt:"desc"}}));}export async function POST(r:NextRequest){const b=await r.json();if(!b.title||!b.opportunity||!b.nextAction)return NextResponse.json({error:"title, opportunity and nextAction required"},{status:400});return NextResponse.json(await prisma.applicationOpportunity.create({data:{title:b.title,opportunity:b.opportunity,value:b.value||null,dataBoundary:b.dataBoundary||null,nextAction:b.nextAction,status:b.status||"active"}}),{status:201});}
"use client";
import {useEffect,useState} from "react";
type E={id:string;title:string;hypothesis:string;hardware:string|null;modelChoice:string|null;dataBoundary:string|null;testSteps:string|null;result:string|null;decision:string|null;nextAction:string|null;status:string};
export default function Lab(){const [items,setItems]=useState<E[]>([]);const [busy,setBusy]=useState(false);const load=()=>fetch('/api/lab').then(r=>r.json()).then(setItems);useEffect(()=>{load()},[]);const add=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setBusy(true);const r=await fetch('/api/lab',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok){e.currentTarget.reset();load()}setBusy(false)};return <main className="max-w-5xl space-y-8 pb-16"><header className="pt-4"><p className="eyebrow">Build to learn</p><h1 className="mt-3 text-4xl font-semibold">Local LLM Lab</h1><p className="mt-3 text-[var(--text-2)]">Record a hypothesis, run a safe test on the 4090, then make a decision.</p></header><form onSubmit={add} className="panel grid gap-3 p-5 md:grid-cols-2"><input name="title" required placeholder="Experiment title" className="input"/><input name="modelChoice" placeholder="Model and runtime" className="input"/><textarea name="hypothesis" required placeholder="Hypothesis" className="input md:col-span-2" rows={2}/><input name="hardware" placeholder="Hardware, e.g. RTX 4090" className="input"/><input name="dataBoundary" placeholder="Security and data boundary" className="input"/><textarea name="setup" placeholder="Local setup requirements" className="input" rows={2}/><textarea name="testSteps" placeholder="Test steps" className="input" rows={2}/><textarea name="nextAction" placeholder="Next action" className="input md:col-span-2" rows={2}/><button className="btn-primary md:col-span-2" disabled={busy}>{busy?'Saving…':'Create experiment'}</button></form><section className="space-y-3">{items.map(i=><article key={i.id} className="panel p-5"><p className="eyebrow">{i.status} · {i.modelChoice||'model not selected'}</p><h2 className="mt-2 text-lg font-semibold">{i.title}</h2><p className="mt-2 text-[var(--text-2)]">{i.hypothesis}</p><dl className="mt-4 grid gap-3 text-sm md:grid-cols-2"><div><dt className="font-semibold">Boundary</dt><dd>{i.dataBoundary||'Define before testing.'}</dd></div><div><dt className="font-semibold">Next action</dt><dd>{i.nextAction||'Choose the next test.'}</dd></div></dl></article>)}</section></main>}

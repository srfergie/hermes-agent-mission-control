"use client";import{useEffect,useState}from"react";type M={id:string;track:string;title:string;nextAction:string;deadline:string|null;status:string};export default function Study(){const[i,setI]=useState<M[]>([]);const l=()=>fetch('/api/study').then(r=>r.json()).then(setI);useEffect(()=>{l()},[]);const s=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const r=await fetch('/api/study',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok){e.currentTarget.reset();l()}};return <main className="max-w-4xl space-y-8 pb-16"><h1 className="pt-4 text-4xl font-semibold">Study Track</h1><form onSubmit={s} className="panel grid gap-3 p-5 md:grid-cols-2"><input name="track" required placeholder="MSc or AI-103" className="input"/><input name="title" required placeholder="Milestone" className="input"/><input name="deadline" type="date" className="input"/><input name="nextAction" required placeholder="One concrete next action" className="input"/><button className="btn-primary md:col-span-2">Add milestone</button></form>{i.map(x=><article key={x.id} className="panel p-5"><p className="eyebrow">{x.track} {x.deadline&&`· due ${new Date(x.deadline).toLocaleDateString('en-GB')}`}</p><h2 className="mt-2 text-lg font-semibold">{x.title}</h2><p className="mt-3 text-[var(--text-2)]">Next action: {x.nextAction}</p></article>)}</main>}
"use client";import{useEffect,useState}from"react";type O={id:string;title:string;opportunity:string;value:string|null;dataBoundary:string|null;nextAction:string;status:string};export default function Application(){const[i,setI]=useState<O[]>([]);const l=()=>fetch('/api/application').then(r=>r.json()).then(setI);useEffect(()=>{l()},[]);const s=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const r=await fetch('/api/application',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok){e.currentTarget.reset();l()}};return <main className="max-w-4xl space-y-8 pb-16"><h1 className="pt-4 text-4xl font-semibold">Practical Application</h1><form onSubmit={s} className="panel grid gap-3 p-5 md:grid-cols-2"><input name="title" required placeholder="Opportunity title" className="input"/><input name="value" placeholder="Expected value" className="input"/><textarea name="opportunity" required placeholder="Opportunity" className="input"/><textarea name="dataBoundary" placeholder="Data boundary" className="input"/><input name="nextAction" required placeholder="One concrete next action" className="input md:col-span-2"/><button className="btn-primary md:col-span-2">Add opportunity</button></form>{i.map(x=><article key={x.id} className="panel p-5"><p className="eyebrow">{x.status}</p><h2 className="mt-2 text-lg font-semibold">{x.title}</h2><p className="mt-2 text-[var(--text-2)]">{x.opportunity}</p><p className="mt-3 text-sm">Next: {x.nextAction}</p></article>)}</main>}